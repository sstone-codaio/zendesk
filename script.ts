window.onload = function() {
  const appContainer = document.querySelector('.apps.workspace');
  const helpDiv = appContainer.appendChild(document.createElement('div'));
  helpDiv.setAttribute("id", "HelpDivContainer");
};
